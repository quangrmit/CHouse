// #include "Database.h"
#include "CLI.h"

int main() {
    // Database * data = Database::getInstance();
    // app.run()
    //delete data;
    // delete app; 
    CLI *test = new CLI();
    test->welcome();
}